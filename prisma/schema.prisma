generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
}

model User {
  id        String   @id @default(uuid())
  email     String   @unique
  name      String
  image     String
  createdAt DateTime @default(now())
  provider  Provider
}

enum Provider {
  Google
}
